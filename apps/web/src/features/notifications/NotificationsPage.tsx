import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query"; import { api, displayDate, post } from "../../lib/api"; import { Badge, Card, Empty, ErrorState, Loading, Page } from "../../components/ui";
type Notification = { id: string; type: string; title: string; body: string; readAt: string | null; createdAt: string };
export function NotificationsPage() { const client = useQueryClient(); const query = useQuery({ queryKey: ["notifications"], queryFn: () => api<Notification[]>("/notifications").then((r) => r.data) }); const read = useMutation({ mutationFn: (id: string) => post(`/notifications/${id}/read`, {}), onSuccess: () => client.invalidateQueries({ queryKey: ["notifications"] }) }); return <Page title="Notifications" description="Workflow updates that need your attention."><Card>{query.isLoading ? <Loading /> : query.error ? <ErrorState error={query.error} /> : !query.data?.length ? <Empty>You’re all caught up.</Empty> : <div className="notification-list">{query.data.map((item) => <article key={item.id} className={item.readAt ? "read" : ""}><span className="notification-icon">◉</span><div><header><strong>{item.title}</strong>{!item.readAt && <Badge tone="info">NEW</Badge>}</header><p>{item.body}</p><small>{displayDate(item.createdAt)} · {item.type.replaceAll("_", " ")}</small></div>{!item.readAt && <button onClick={() => read.mutate(item.id)}>Mark read</button>}</article>)}</div>}</Card></Page>; }
