import { useState } from "react"; import { useLocation, useNavigate } from "react-router"; import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query"; import { api, patch, post, put } from "../../lib/api"; import { Badge, Card, Empty, ErrorState, Field, Loading, Modal, Page, statusTone } from "../../components/ui";
type Master = { id: string; code: string; name: string; status: string; type?: string; description?: string | null; parentDepartmentId?: string | null; parentLocationId?: string | null; defaultUsefulLifeMonths?: number | null };
const tabs = [{ key: "departments", label: "Departments" }, { key: "locations", label: "Locations" }, { key: "categories", label: "Categories" }];
export function OrganizationSetupPage() { const location = useLocation(); const navigate = useNavigate(); const key = tabs.find((tab) => location.pathname.endsWith(tab.key))?.key ?? "departments"; const queryClient = useQueryClient(); const [open, setOpen] = useState(false); const query = useQuery({ queryKey: [key], queryFn: () => api<Master[]>(`/${key}`).then((r) => r.data) }); const create = useMutation({ mutationFn: (body: Record<string, unknown>) => post(`/${key}`, body), onSuccess: () => { queryClient.invalidateQueries({ queryKey: [key] }); setOpen(false); } }); return <Page title="Organization setup" description="Define the master data used by every AssetFlow workflow." action={<button className="primary" onClick={() => setOpen(true)}>＋ Add {key.slice(0, -1)}</button>}><div className="tabs">{tabs.map((tab) => <button className={key === tab.key ? "active" : ""} key={tab.key} onClick={() => navigate(`/organization/${tab.key}`)}>{tab.label}</button>)}</div><Card>{query.isLoading ? <Loading /> : query.error ? <ErrorState error={query.error} /> : !query.data?.length ? <Empty>No {key} configured.</Empty> : <table><thead><tr><th>Code</th><th>Name</th><th>Type / detail</th><th>Status</th></tr></thead><tbody>{query.data.map((item) => <tr key={item.id}><td><strong>{item.code}</strong></td><td>{item.name}</td><td>{item.type ?? item.description ?? (item.defaultUsefulLifeMonths ? `${item.defaultUsefulLifeMonths} months useful life` : "—")}</td><td><Badge tone={statusTone(item.status)}>{item.status}</Badge></td></tr>)}</tbody></table>}</Card><MasterModal kind={key} open={open} onClose={() => setOpen(false)} items={query.data ?? []} pending={create.isPending} error={create.error} onSubmit={(values) => create.mutate(values)} /></Page>; }
function MasterModal({ kind, open, onClose, onSubmit, pending, error, items }: { kind: string; open: boolean; onClose(): void; onSubmit(v: Record<string, unknown>): void; pending: boolean; error: unknown; items: Master[] }) { return <Modal title={`Add ${kind.slice(0, -1)}`} open={open} onClose={onClose}><form onSubmit={(event) => { event.preventDefault(); const values = Object.fromEntries(new FormData(event.currentTarget)); onSubmit({ ...values, ...(kind === "locations" ? { type: values.type || "AREA", parentLocationId: values.parentLocationId || undefined } : {}), ...(kind === "departments" ? { parentDepartmentId: values.parentDepartmentId || undefined } : {}), ...(kind === "categories" && values.defaultUsefulLifeMonths ? { defaultUsefulLifeMonths: Number(values.defaultUsefulLifeMonths) } : {}) }); }}><div className="form-grid"><Field label="Code"><input name="code" required /></Field><Field label="Name"><input name="name" required /></Field></div>{kind === "locations" && <><Field label="Location type"><select name="type"><option>SITE</option><option>BUILDING</option><option>FLOOR</option><option>ROOM</option><option>AREA</option></select></Field><Field label="Parent location"><select name="parentLocationId"><option value="">No parent</option>{items.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field></>}{kind === "departments" && <Field label="Parent department"><select name="parentDepartmentId"><option value="">No parent</option>{items.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field>}{kind === "categories" && <Field label="Default useful life (months)"><input name="defaultUsefulLifeMonths" type="number" min="1" /></Field>}<Field label="Description"><textarea name="description" rows={3}/></Field>{Boolean(error) && <p className="form-error">{error instanceof Error ? error.message : "Unable to save."}</p>}<div className="form-actions"><button type="button" onClick={onClose}>Cancel</button><button className="primary" disabled={pending}>Save</button></div></form></Modal>; }
type Employee = { id: string; firstName: string; lastName: string | null; email: string; employeeCode: string | null; status: string; roles: Array<{ role: string; departmentId: string | null }> };
export function EmployeesPage() { const client = useQueryClient(); const [selected, setSelected] = useState<Employee | null>(null); const query = useQuery({ queryKey: ["employees"], queryFn: () => api<Employee[]>("/employees?pageSize=100").then((r) => r.data) }); const roles = useMutation({ mutationFn: ({ id, roles }: { id: string; roles: Array<{ role: string }> }) => put(`/employees/${id}/roles`, roles), onSuccess: () => { client.invalidateQueries({ queryKey: ["employees"] }); setSelected(null); } }); const status = useMutation({ mutationFn: ({ id, status }: { id: string; status: string }) => patch(`/employees/${id}`, { status }), onSuccess: () => client.invalidateQueries({ queryKey: ["employees"] }) }); return <Page title="Employees" description="Manage access, roles, and employee account status."><Card>{query.isLoading ? <Loading /> : query.error ? <ErrorState error={query.error} /> : !query.data?.length ? <Empty /> : <table><thead><tr><th>Employee</th><th>Email</th><th>Roles</th><th>Status</th><th/></tr></thead><tbody>{query.data.map((employee) => <tr key={employee.id}><td><strong>{employee.firstName} {employee.lastName}</strong><small className="block">{employee.employeeCode}</small></td><td>{employee.email}</td><td><div className="tag-row">{employee.roles.map((role) => <Badge key={role.role} tone="info">{role.role.replaceAll("_", " ")}</Badge>)}</div></td><td><Badge tone={statusTone(employee.status)}>{employee.status}</Badge></td><td className="actions"><button onClick={() => setSelected(employee)}>Roles</button><button onClick={() => status.mutate({ id: employee.id, status: employee.status === "ACTIVE" ? "INACTIVE" : "ACTIVE" })}>{employee.status === "ACTIVE" ? "Deactivate" : "Activate"}</button></td></tr>)}</tbody></table>}</Card><Modal title="Assign roles" open={Boolean(selected)} onClose={() => setSelected(null)}><form onSubmit={(event) => { event.preventDefault(); const selectedRoles = new FormData(event.currentTarget).getAll("roles").map((role) => ({ role: String(role) })); if (selected) roles.mutate({ id: selected.id, roles: selectedRoles }); }}><p>Employee access is always included. Add only the elevated permissions this person needs.</p>{["EMPLOYEE", "ADMIN", "ASSET_MANAGER", "DEPARTMENT_HEAD"].map((role) => <label className="check-row" key={role}><input type="checkbox" name="roles" value={role} defaultChecked={selected?.roles.some((item) => item.role === role)} />{role.replaceAll("_", " ")}</label>)}{roles.error && <p className="form-error">{roles.error.message}</p>}<div className="form-actions"><button type="button" onClick={() => setSelected(null)}>Cancel</button><button className="primary">Update roles</button></div></form></Modal></Page>; }
