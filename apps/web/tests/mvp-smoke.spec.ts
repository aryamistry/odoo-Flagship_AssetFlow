import { expect, test, type Page } from "@playwright/test";
const actor = { id: "user-1", organizationId: "org-1", email: "admin@assetflow.local", firstName: "Aarav", lastName: "Admin", primaryDepartmentId: null, roles: [{ role: "EMPLOYEE", departmentId: null }, { role: "ADMIN", departmentId: null }, { role: "ASSET_MANAGER", departmentId: null }] };
async function mockApi(page: Page) { let authenticated = false; await page.route("**/api/v1/**", async (route) => { const url = new URL(route.request().url()); const path = url.pathname.replace("/api/v1", ""); if (path === "/auth/me") return route.fulfill({ status: authenticated ? 200 : 401, json: authenticated ? { data: actor, meta: { requestId: "test" } } : { code: "AUTHENTICATION_REQUIRED" } }); if (path === "/auth/login") { authenticated = true; return route.fulfill({ json: { data: actor, meta: { requestId: "test" } } }); } if (path === "/dashboard/summary") return route.fulfill({ json: { data: { kpis: { available: 4, allocated: 1, maintenanceToday: 2, activeBookings: 1, pendingTransfers: 1, upcomingReturns: 0 }, overdueReturns: 1, recentActivity: [] }, meta: { requestId: "test" } } }); return route.fulfill({ json: { data: [], meta: { requestId: "test", page: 1, pageSize: 25, total: 0 } } }); }); }
test("login exposes role-aware navigation and live dashboard cards", async ({ page }) => { await mockApi(page); await page.goto("/login"); await page.getByRole("button", { name: "Sign in" }).click(); await expect(page.getByRole("heading", { name: "Good morning" })).toBeVisible(); await expect(page.getByText("Available", { exact: true })).toBeVisible(); await expect(page.getByRole("link", { name: /Setup/ })).toBeVisible(); });
test("maintenance route renders the required five workflow columns", async ({ page }) => { await mockApi(page); await page.goto("/login"); await page.getByRole("button", { name: "Sign in" }).click(); await page.getByRole("link", { name: /Maintenance/ }).click(); for (const column of ["PENDING", "APPROVED", "TECHNICIAN ASSIGNED", "IN PROGRESS", "RESOLVED"]) await expect(page.getByRole("heading", { name: column })).toBeVisible(); });
